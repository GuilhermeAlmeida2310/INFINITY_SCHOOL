# Crie um programa que peça ao usuário para digitar uma palavra. Em seguida, utilize um laço for para percorrer cada letra da string e exibi-la uma por linha.

palavra = input('Digite uma palavra: ')

for letras in palavra:
    print(letras)